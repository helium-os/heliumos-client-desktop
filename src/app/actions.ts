'use server';

const { getBinaryVersion, getClusterConfig, installHeliumos, getInstallStatus } = require('../../proxy/install');

export { getBinaryVersion, getClusterConfig, installHeliumos, getInstallStatus };
